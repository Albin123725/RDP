services:
  - type: web
    name: vnc-desktop
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    # NO health check until it works
    # healthCheckPath: /
    envVars:
      - key: VNC_PASSWD
        value: "password123"
    resources:
      memoryMB: 512

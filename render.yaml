services:
  - type: web  # Free tier supports this
    name: vnc-desktop
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    healthCheckPath: /
    envVars:
      - key: VNC_PASSWORD
        value: "password123"
    resources:
      memoryMB: 512

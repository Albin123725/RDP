services:
  - type: worker  # MUST be worker, not web
    name: vnc-desktop
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    envVars:
      - key: VNC_PASSWORD
        value: "password123"
    resources:
      memoryMB: 512

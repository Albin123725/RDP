services:
  - type: web
    name: vnc-desktop
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    healthCheckPath: /vnc.html
    envVars:
      - key: VNC_PASSWORD
        value: "yourpassword123"
    resources:
      memoryMB: 512

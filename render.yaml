services:
  - type: worker  # CRITICAL: Worker allows direct TCP port access
    name: vnc-desktop
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    envVars:
      - key: VNC_PASSWORD
        value: "password123"
    resources:
      memoryMB: 512

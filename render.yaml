services:
  - type: web
    name: codesandbox-real-browser
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 10000
    healthCheckPath: /health
    autoDeploy: true

services:
  - type: web
    name: vnc-desktop
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    envVars:
      - key: VNC_PASSWD
        value: "password123"
      - key: VNC_RESOLUTION
        value: "800x600"
      - key: VNC_DEPTH  
        value: "16"
    resources:
      memoryMB: 512

# render.yaml - SIMPLE WORKING VERSION
services:
  - type: web
    name: novnc-rdp
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    envVars:
      - key: VNC_PASSWORD
        value: "password123"
    healthCheckPath: /

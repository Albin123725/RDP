services:
  - type: web
    name: real-browser
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 8080
    healthCheckPath: /health
    autoDeploy: true
    disk:
      name: chrome-data
      mountPath: /data
      sizeGB: 1

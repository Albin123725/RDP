services:
  - type: web
    name: vnc-proxy
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    autoDeploy: true
    envVars:
      - key: VNC_PASSWORD
        value: "yourpassword123"
    resources:
      memoryMB: 512
